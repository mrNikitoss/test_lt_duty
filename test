# Создаем словарь, где ключ - дата, значение - имя сотрудника
work_schedule = {
    "01-01-2023": "Иванов",
    "02-01-2023": "Петров",
    "03-01-2023": "Сидоров",
    # добавьте остальные даты и имена сотрудников
}

# Запрашиваем у пользователя ввод даты
date = input("Введите дату в формате ДД-ММ-ГГГГ: ")

# Проверяем, есть ли такая дата в расписании
if date in work_schedule:
    print(f"В эту дату работает {work_schedule[date]}")
else:
    print("Информация о работе в эту дату отсутствует")
